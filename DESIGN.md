# PB Zero Wiki — 设计系统

> 给 AI 编码 Agent 读的设计规范。每次生成或修改 UI 前，Agent 必须逐段理解本文件的约束。
> 版本: 2.0 | 最后更新: 2026-06-09

---

## 1. 视觉主题与氛围

**一句话**: 中国武侠动作 RPG Wiki — 暗黑、东方美学、锋利感。

- **密度**: 中高。玩家来查武学招式、Boss 机制、装备锻造。
- **气质**: 暗黑中国风 + 硬核动作游戏。橙色主色如刀光剑影中的火花，深色背景如水墨中的留白。
- **情绪**: 专业、硬核、东方武者社区感。

---

## 2. 色彩系统

| Token | Hex | 用途 |
|-------|-----|------|
| `--color-bg` | `#0b0b14` | 页面底色 |
| `--color-surface` | `#14142a` | 卡片、模态框 |
| `--color-surface-elevated` | `#181835` | 悬浮态表面 |
| `--color-border` | `#2a2a3a` | 默认边框 |
| `--color-border-hover` | `#ff6b35` | hover 态边框 |
| `--color-primary` | `#ff6b35` | 主强调色（刀锋橙） |
| `--color-primary-hover` | `#e85d2a` | 主色 hover |
| `--color-primary-muted` | `rgba(255,107,53,0.12)` | 主色弱化背景 |
| `--color-text` | `#e0e0e0` | 正文 |
| `--color-text-heading` | `#ffffff` | 标题 |
| `--color-text-muted` | `#a0a0b0` | 辅助文字 |
| `--color-text-dim` | `#6a6a7a` | 禁用/占位 |
| `--color-danger` | `#ef4444` | 删除/危险 |
| `--color-warning` | `#f59e0b` | 警告 |
| `--color-success` | `#22c55e` | 成功 |

**约束**: 主色只做强调，如刀刃上的火光。正文不用纯白。卡片背景统一 `#14142a`。

---

## 3. 排版规则

字体: `-apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif`

| 层级 | 字号 | 字重 | 行高 |
|------|------|------|------|
| Hero | 40px | 700 | 1.2 |
| H1 | 32px | 700 | 1.3 |
| H2 | 22px | 600 | 1.4 |
| H3 | 17px | 600 | 1.5 |
| Body | 15px | 400 | 1.65 |

---

## 4. 组件样式

**按钮**: 橙色实色 `#ff6b35`，hover `#e85d2a`。**卡片**: 背景 `#14142a`，边框 `#2a2a3a`，圆角 10px，hover 橙色边框。**导航**: 粘性 + 毛玻璃。

---

## 5. 布局原则

4px 倍数间距。内容最大宽度 1040px。卡片 gap 20px。

---

## 6. 该做和不该做

### ✅ 该做
- 橙色 `#ff6b35` 做视觉锚点
- 暗色渐变保持东方水墨感

### ❌ 不该做
- 不用绿/蓝/金色做强调
- 不用彩色渐变 Hero
- 不用弹性缓动动画
- 不用 emoji 替代图标
- 不引入第三方 CSS 框架

---

## 7. Agent 提示指南

**颜色速查**: 背景 `#0b0b14` | 卡片 `#14142a` | 边框 `#2a2a3a` | 主色 `#ff6b35` | 标题 `#fff` | 正文 `#e0e0e0`

**修改前检查**: 无其他色系强调色 | 卡片 `#14142a` | 无 scale 动画 | 无第三方 CSS
